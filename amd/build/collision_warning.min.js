/**
 * Tells the author, while they are still choosing, that this repeating notice would compete with
 * another one for the same pages.
 *
 * Notices are shown one at a time, so two repeating notices aimed at the same pages take turns
 * interrupting the same people. Saving already warns about it; this says so early enough to change
 * the answer instead of having to come back.
 *
 * @module     local_awareness/collision_warning
 * @copyright  2026 Anderson Blaine
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_awareness/collision_warning",["core/ajax","core/str","core/notification"],(function(Ajax,Str,Notification){var SELECTORS_pathmatch="#id_pathmatch",SELECTORS_resetinterval="#id_resetinterval_number",SELECTORS_slotHost="#fitem_id_pathmatch",SELECTORS_noticeid="#id_id",state={noticeid:0,slot:null,timer:null,sequence:0},render=function(titles){var slot=function(){if(state.slot)return state.slot;var host=document.querySelector(SELECTORS_slotHost);if(!host)return null;var slot=document.createElement("div");return slot.className="alert alert-warning mt-2",slot.setAttribute("role","status"),slot.hidden=!0,host.appendChild(slot),state.slot=slot,slot}();if(slot)return titles.length?void Str.get_string("collision:live","local_awareness",titles.join(", ")).then((function(message){return slot.textContent=message,slot.hidden=!1,null})).catch(Notification.exception):(slot.hidden=!0,void(slot.textContent=""))},check=function(){var pathfield=document.querySelector(SELECTORS_pathmatch);if(pathfield)if((field=document.querySelector(SELECTORS_resetinterval))&&parseInt(field.value,10)>0){var field,root,mine=++state.sequence;Ajax.call([{methodname:"local_awareness_check_collision",args:{noticeid:state.noticeid,courseid:(root=document.querySelector('[data-region="la-editor"]'),root&&parseInt(root.getAttribute("data-courseid"),10)||0),pathmatch:pathfield.value,repeats:!0}}])[0].then((function(response){return mine===state.sequence&&render(response.titles||[]),null})).catch(Notification.exception)}else render([])},schedule=function(){state.timer&&window.clearTimeout(state.timer),state.timer=window.setTimeout(check,400)};return{init:function(){var idfield=document.querySelector(SELECTORS_noticeid);state.noticeid=idfield&&parseInt(idfield.value,10)||0;var pathfield=document.querySelector(SELECTORS_pathmatch),intervalfield=document.querySelector(SELECTORS_resetinterval);pathfield&&(pathfield.addEventListener("input",schedule),intervalfield&&(intervalfield.addEventListener("input",schedule),intervalfield.addEventListener("change",schedule)),check())}}}));

//# sourceMappingURL=collision_warning.min.js.map