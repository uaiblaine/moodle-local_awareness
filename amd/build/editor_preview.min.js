define("local_awareness/editor_preview",["exports","core/modal_cancel","core/notification","core/pending","core/str"],(function(_exports,_modal_cancel,_notification,_pending,_str){function _interopRequireDefault(obj){return obj&&obj.__esModule?obj:{default:obj}}
/**
   * Preview the notice being edited, as the reader will get it.
   *
   * This replaces a hand-drawn mock of the notice modal that was itself rendered inside a core modal
   * — a picture of a dialogue, inside a dialogue, showing truncated plain text with the formatting
   * stripped. It was not a preview; it was an illustration that had to be kept in step with the real
   * thing by hand, and it drifted the moment either changed.
   *
   * The same shape the manage list uses for its own preview: the content goes into a
   * core/modal_cancel, which is what the reader eventually sees it in. The difference is where the
   * content comes from — the list has a saved notice, and this has a form that may never have been
   * saved, so it reads the editor rather than the database.
   *
   * @module     local_awareness/editor_preview
   * @copyright  2026 Anderson Blaine
   * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
   */Object.defineProperty(_exports,"__esModule",{value:!0}),_exports.init=void 0,_modal_cancel=_interopRequireDefault(_modal_cancel),_notification=_interopRequireDefault(_notification),_pending=_interopRequireDefault(_pending);const SELECTORS_editor='[data-region="la-editor"]',SELECTORS_trigger='[data-action="preview"]',SELECTORS_title='[name="title"]',getContent=()=>{const editable=document.querySelector("#id_contenteditable");if(editable)return editable.innerHTML;if(window.tinymce&&window.tinymce.get("id_content"))return window.tinymce.get("id_content").getContent();const textarea=document.getElementById("id_content");return textarea?textarea.value:""};_exports.init=()=>{const editor=document.querySelector(SELECTORS_editor);if(!editor)return;const trigger=editor.querySelector(SELECTORS_trigger);trigger&&trigger.addEventListener("click",(event=>{event.preventDefault(),(async trigger=>{const pending=new _pending.default("local_awareness/editor_preview:open");try{const titlefield=document.querySelector(SELECTORS_title),title=titlefield&&""!==titlefield.value.trim()?titlefield.value.trim():await(0,_str.getString)("editor:preview:title","local_awareness"),body=""!==getContent().trim()?getContent():await(0,_str.getString)("editor:preview:empty","local_awareness"),modal=await _modal_cancel.default.create({title:title,body:body,large:!0,returnElement:trigger,removeOnClose:!0});await modal.show()}finally{pending.resolve()}})(trigger).catch(_notification.default.exception)}))}}));

//# sourceMappingURL=editor_preview.min.js.map