/**
 * Opens the notice as the reader will see it, from the editor.
 *
 * Puts the real content into a core/modal_cancel rather than drawing a mock of the dialogue, so what
 * the author checks is the thing that ships.
 *
 * @module     local_awareness/preview
 * @copyright  2026 Anderson Blaine
 * @license    http://www.gnu.org/copyleft/gpl.html GNU GPL v3 or later
 */
define("local_awareness/preview",["jquery","core/modal_cancel","core/str"],(function($,ModalCancel,str){var preview={init:function(){$("a.notice-preview").on("click",(function(e){var content=$(e.currentTarget).attr("data-noticecontent");return ModalCancel.create({title:str.get_string("notice:content","local_awareness"),body:content,large:!0}).then((function(modal){return modal.show()}))}))}};return preview}));

//# sourceMappingURL=preview.min.js.map