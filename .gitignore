# OS files
.DS_Store

# IDE / Editor
.vscode/

# Node.js (build tooling only - not part of the plugin)
node_modules/
package.json
package-lock.json

# Claude Code scratch worktrees. One of these was once committed as a bare gitlink
# (mode 160000) with no .gitmodules entry, which made every CI checkout emit
# "fatal: No url found for submodule path".
.claude/worktrees/
